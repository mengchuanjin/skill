# 复制成 .env.local 后按需修改。所有变量都是可选的。
# 部署到 Vercel 时，这些要填在 Vercel 项目的 Settings → Environment Variables 里，
# .env.local 不会被提交、也不会被 Vercel 读到。

# ---- GitHub（「最新 Skill / 工具」板块）----
# 未认证时 60 次/小时，且是按 IP 算的。本地够用，
# 但 Vercel 上出口 IP 是和别的项目共享的，几乎必然会被限流，
# 所以部署到 Vercel 强烈建议填一个只读 token（不需要勾任何 scope）。
# 刻意不叫 GITHUB_TOKEN —— 很多环境里已经存在一个用途不同的同名变量。
# DASHBOARD_GITHUB_TOKEN=ghp_xxx

# ---- X（「全球看中国」板块，可选）----
# X 在 2023 年取消了免费的搜索 API，recent search 属于付费档位。
# 不填的话该源显示为「未启用」的灰点，不影响其它源，
# 板块里的社交平台观感由 Reddit 和 Bluesky（都免费免登录）承担。
# X_BEARER_TOKEN=

# ---- 调试用 ----
# 前端自动刷新间隔（分钟），默认 45。
# 想验证自动刷新逻辑时把它调小，例如 0.1（=6 秒）。
# NEXT_PUBLIC_REFRESH_INTERVAL_MINUTES=45

# 置为 1 时用内置的离线示例数据，不打任何第三方接口。
# 适合断网开发、或第三方 API 限流时调前端。页面顶部会显示醒目提示。
# 注意：别在 Vercel 上设这个，否则线上看到的全是假数据。
# DASHBOARD_FIXTURES=1
